{
  "name": "peer-edit-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "PeerEdit desktop shell — bundles the relay server and editor UI into a single self-contained exe",
  "main": "entry.js",
  "author": "PeerEdit",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "electron": "33.4.11",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2"
  }
}
